<h2 class="title">Create a Course</h2>

<%= form_for :course do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<br>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<br>
	
	<%= f.submit %>
<% end %>
<h2 class='title'>Students of MIPT</h2>

<ul>
<% @students.each do |student| %>
  <li><%= student.last_name %>, <%= student.first_name %><br>
  <% if student.teacher %>
    <em>Current teacher: <%=  student.teacher.full_name %></em>
  <% end %>
  </li><br>
<% end %>
</ul>
